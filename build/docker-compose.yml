version: '3.1'

services:
  api:
    image: 'everdura/mk8api'
    environment:
      PORT: 3000
      DB: 'mongodb://mongo:27017/mk8'
    ports:
      - 3000:3000
    links:
      - mongo
  mongo:
    image: mongo
    restart: always
    ports:
      - 27017
    environment:
      - MONGODB_DATABASE=mk8
